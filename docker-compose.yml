services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
      - "127.0.0.1:4000:4000"
    env_file:
      - .env.production
    volumes:
      - ./backend/data:/app/backend/data
