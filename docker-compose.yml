services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "4000:4000"
    environment:
      JWT_SECRET: change-me
      NEXT_PUBLIC_API_URL: http://localhost:4000
    volumes:
      - ./backend/data:/app/backend/data
